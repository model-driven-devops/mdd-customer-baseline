name: CI-pipeline

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

env:
  NSO_URL: http://ip-172-22-0-225.us-gov-west-1.compute.internal:8080

jobs:
  test:
    runs-on: self-hosted
    environment: army-uno
    concurrency: army-uno
    container:
      image: ghcr.io/model-driven-devops/mdd:latest
    steps:
      - name: Checkout Inventory
        uses: actions/checkout@v2

      - name: Run YAMLLINT in mdd-data
        run: |
          cd mdd/mdd-data
          yamllint .
      
      - name: Save Rollback
        run:  |
          cd mdd
          ansible-playbook ciscops.mdd.nso_save_rollback
